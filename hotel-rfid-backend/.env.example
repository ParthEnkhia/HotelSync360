DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=hotel_movement_tracker
PORT=5000
JWT_SECRET=change_me
# Comma-separated list, e.g. http://localhost:5173,http://localhost:3000
CORS_ORIGINS=http://localhost:5173
# If true, all non-/auth routes require a valid JWT in Authorization header
AUTH_REQUIRED=false
